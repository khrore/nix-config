#!/bin/bash

# Finish the installation of Omarchy with items that can only be done after logging in.

set -e

FIRST_RUN_MODE=~/.local/state/linux-desktop/first-run.mode

if [[ -f $FIRST_RUN_MODE ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$HOME/.local/share/linux-desktop/install/first-run/battery-monitor.sh"
  bash "$HOME/.local/share/linux-desktop/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$HOME/.local/share/linux-desktop/install/first-run/firewall.sh"
  bash "$HOME/.local/share/linux-desktop/install/first-run/dns-resolver.sh"
  bash "$HOME/.local/share/linux-desktop/install/first-run/gnome-theme.sh"
  bash "$HOME/.local/share/linux-desktop/install/first-run/elephant.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$HOME/.local/share/linux-desktop/install/first-run/welcome.sh"
  bash "$HOME/.local/share/linux-desktop/install/first-run/wifi.sh"
fi
