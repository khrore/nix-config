#!/bin/bash

set -e

echo -e "\e[32mUpdate Omarchy\e[0m"

omarchy-update-time

git -C $HOME/.local/share/linux-desktop pull --autostash
git -C $HOME/.local/share/linux-desktop --no-pager diff --check || git -C $HOME/.local/share/linux-desktop reset --merge
